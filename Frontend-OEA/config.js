// config.js
const CONFIG = {
    // Si estás probando en tu PC, usa localhost. Si ya está en producción, usa tu enlace de Render.
    // O puedes dejar directamente la URL de Render para que consuma siempre la nube:
    API_URL: 'https://oeaguayas.onrender.com'
};